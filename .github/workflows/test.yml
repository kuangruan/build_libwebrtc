name: test
on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: dry-run contracts
        run: bash tests/test_plan.sh
